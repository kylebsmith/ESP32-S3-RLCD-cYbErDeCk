# Metrology + validation toolchain for the cYbErDeCk datum set.
# Pinned to major versions that were verified to reproduce the committed report.
trimesh>=4.0
numpy>=1.24
scipy>=1.10
shapely>=2.0
ezdxf>=1.1
